trigger:
- main

variables:
- group: kv-secrets-vikram

pool:
  vmImage: 'windows-latest'

steps:
- script: echo "Pipeline is running!"
  displayName: "Hello step"

- script: echo "Secret = $(ConnStr))"
  displayName: "Print secret"
